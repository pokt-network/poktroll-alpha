# Don't commit binaries
bin/*
!bin/.keep

# TMP dir
tmp/*
!tmp/.keep

# Why: poktrolld CLI should use the same keys as the node that runs inside the cluster to
# allow commands such as `poktrolld tx stake` for work.
localnet/poktrolld/*
!localnet/poktrolld/keyring-test/
!localnet/poktrolld/keyring-test/*
!localnet/*/config/
!localnet/*/config/*

# Macos
.DS_Store

# Development
.vscode
.env

**/*.pb.go
**/*.pb.gw.go
